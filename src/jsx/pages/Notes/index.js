import React, { Fragment } from "react";
import NoteList from "../../components/Note/NoteList";

const Notes = () => {
  return (
    <Fragment>
      <NoteList />
    </Fragment>
  );
};

export default Notes;
