import React, { Fragment } from "react";
import Sites from "../../components/Site/SiteList";

const SiteList = () => {
  return (
    <Fragment>
      <Sites />
    </Fragment>
  );
};

export default SiteList;
